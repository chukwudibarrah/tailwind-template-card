/** Element name used in dashboards as `custom:tailwind-template-card`. */
export const CARD_TYPE = 'tailwind-template-card'

/** Visual editor element for the card. */
export const CONFIG_TYPE = 'tailwind-template-card-config'

/** Upstream's element name, kept registered for backwards compatibility. */
export const LEGACY_CARD_TYPE = 'tailwindcss-template-card'
